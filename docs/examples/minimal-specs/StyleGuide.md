# Minimal Kernel Style Guide

```json
{
  "language_rules": {
    "python_version": ">=3.11",
    "type_hints": "required"
  },
  "format_rules": {
    "line_length": 100
  },
  "review_checkpoints": [
    "Kernel must remain business-agnostic",
    "Cross-agent coordination must happen through EventBus"
  ]
}
```
