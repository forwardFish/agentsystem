# verifier

## 技能描述
执行项目已配置的本地校验命令，返回结构化验证结果。

## 输入参数
- commands
- target_file

## 执行规则
- 只允许执行项目配置文件里已有的命令
- 目标文件是前端文件时优先执行前端格式化命令
- 任何命令失败都必须保留原始错误输出摘要

## 输出要求
- 输出结构化命令结果
- 明确给出 final_status

## 绝对禁止事项
- 不允许执行配置之外的命令
- 不允许静默忽略失败
