task_id: S1-001
task_name: 交割单上传 API
sprint: Sprint 1
epic: Epic 1.1 交割单摄入
story_id: S1-001
blast_radius: L2
execution_mode: Safe
mode: Safe
goal: 实现 CSV/XLSX 文件上传并落对象存储。
business_value: 让 交割单上传 API 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 无前置依赖。
acceptance_criteria:
- 交割单上传 API 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/routes/statements.py'
out_of_scope:
- 不在本 Story 中实现 交割单上传 API 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 交割单上传 API 之外的后续能力。
- 不做架构级重构。
dependencies:
- 无
related_files:
- apps/api/src/routes/statements.py
- apps/api/src/modules/statements/交割单上传_api.py
primary_files:
- apps/api/src/routes/statements.py
secondary_files:
- apps/api/src/modules/statements/交割单上传_api.py
test_cases:
  normal:
  - 正常路径：交割单上传 API 的核心场景可以完成。
  exception:
  - 异常路径：交割单上传 API 失败时返回可解释结果。
