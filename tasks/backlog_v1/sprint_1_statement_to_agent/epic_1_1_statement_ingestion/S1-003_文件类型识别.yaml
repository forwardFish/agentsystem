task_id: S1-003
task_name: 文件类型识别
sprint: Sprint 1
epic: Epic 1.1 交割单摄入
story_id: S1-003
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 识别 csv/xlsx 并路由到正确解析器。
business_value: 让 文件类型识别 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- 文件类型识别 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/routes/statements.py'
out_of_scope:
- 不在本 Story 中实现 文件类型识别 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 文件类型识别 之外的后续能力。
- 不做架构级重构。
dependencies:
- S1-002
related_files:
- apps/api/src/routes/statements.py
- apps/api/src/modules/statements/文件类型识别.py
primary_files:
- apps/api/src/routes/statements.py
secondary_files:
- apps/api/src/modules/statements/文件类型识别.py
test_cases:
  normal:
  - 正常路径：文件类型识别 的核心场景可以完成。
  exception:
  - 异常路径：文件类型识别 失败时返回可解释结果。
