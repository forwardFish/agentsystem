task_id: S1-007
task_name: 解析校验与错误报告
sprint: Sprint 1
epic: Epic 1.2 交割单解析与标准化
story_id: S1-007
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 输出结构化解析结果与错误报告。
business_value: 让 解析校验与错误报告 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- 解析校验与错误报告 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/statements/解析校验与错误报告.py'
out_of_scope:
- 不在本 Story 中实现 解析校验与错误报告 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 解析校验与错误报告 之外的后续能力。
- 不做架构级重构。
dependencies:
- S1-006
related_files:
- apps/api/src/modules/statements/解析校验与错误报告.py
- apps/api/src/modules/statements/mappers/__init__.py
primary_files:
- apps/api/src/modules/statements/解析校验与错误报告.py
secondary_files:
- apps/api/src/modules/statements/mappers/__init__.py
test_cases:
  normal:
  - 正常路径：解析校验与错误报告 的核心场景可以完成。
  exception:
  - 异常路径：解析校验与错误报告 失败时返回可解释结果。
