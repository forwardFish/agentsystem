task_id: S1-010
task_name: Agent 创建 API
sprint: Sprint 1
epic: Epic 1.3 Agent DNA / Profile
story_id: S1-010
blast_radius: L2
execution_mode: Safe
mode: Safe
goal: 基于 statement/profile/init_cash 创建 Agent。
business_value: 让 Agent 创建 API 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- Agent 创建 API 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩大到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/profile/agent_创建_api.py'
out_of_scope:
- 不在本 Story 中实现 Agent 创建 API 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 Agent 创建 API 之外的后续能力。
- 不做架构级重构。
dependencies:
- S1-009
related_files:
- apps/api/src/modules/profile/agent_创建_api.py
- docs/contracts/trading_agent_profile.schema.json
primary_files:
- apps/api/src/modules/profile/agent_创建_api.py
secondary_files:
- docs/contracts/trading_agent_profile.schema.json
test_cases:
  normal:
  - 正常路径：Agent 创建 API 的核心场景可以完成。
  exception:
  - 异常路径：Agent 创建 API 失败时返回可解释结果。
