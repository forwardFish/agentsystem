task_id: S2-001
task_name: 交易日历同步
sprint: Sprint 2
epic: Epic 2.1 市场日历与行情
story_id: S2-001
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 接入 trade_cal。
business_value: 让 交易日历同步 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 无前置依赖。
acceptance_criteria:
- 交易日历同步 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/world/交易日历同步.py'
out_of_scope:
- 不在本 Story 中实现 交易日历同步 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 交易日历同步 之外的后续能力。
- 不做架构级重构。
dependencies:
- 无
related_files:
- apps/api/src/modules/world/交易日历同步.py
- apps/api/src/modules/world/service.py
primary_files:
- apps/api/src/modules/world/交易日历同步.py
secondary_files:
- apps/api/src/modules/world/service.py
story_inputs:
  - "trade_cal 数据源与日历同步规则。"
  - "world service 读取交易日历所需的数据结构。"
  - "本地缓存和持久化交易日信息的目标格式。"

story_process:
  - "接入并拉取 trade_cal 交易日历。"
  - "把交易日信息写入本地可复用存储。"
  - "让 world service 能稳定读取交易日日历。"

story_outputs:
  - "已同步的交易日历数据。"
  - "可供后续交易日推进直接使用的日历基础。"

verification_basis:
  - "检查交易日历可以成功同步。"
  - "检查 world service 能读取同步结果。"
  - "检查后续交易日推进链路可直接消费这些数据。"

test_cases:
  normal:
  - 正常路径：交易日历同步 的核心场景可以完成。
  exception:
  - 异常路径：交易日历同步 失败时返回可解释结果。
