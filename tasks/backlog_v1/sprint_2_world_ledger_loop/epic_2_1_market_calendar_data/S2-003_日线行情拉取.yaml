task_id: S2-003
task_name: 日线行情拉取
sprint: Sprint 2
epic: Epic 2.1 市场日历与行情
story_id: S2-003
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 接入 daily 行情数据。
business_value: 让 日线行情拉取 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- 日线行情拉取 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/world/日线行情拉取.py'
out_of_scope:
- 不在本 Story 中实现 日线行情拉取 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 日线行情拉取 之外的后续能力。
- 不做架构级重构。
dependencies:
- S2-002
related_files:
- apps/api/src/modules/world/日线行情拉取.py
- apps/api/src/modules/world/service.py
primary_files:
- apps/api/src/modules/world/日线行情拉取.py
secondary_files:
- apps/api/src/modules/world/service.py
story_inputs:
  - "交易日推进结果和目标交易标的列表。"
  - "daily 行情数据源与拉取规则。"
  - "world service 需要的基础行情字段。"

story_process:
  - "按交易日拉取日线行情数据。"
  - "把行情整理成 world service 可消费的结构。"
  - "为后续快照与撮合保留价格数据。"

story_outputs:
  - "已拉取的日线行情数据。"
  - "可供 world snapshot 和撮合模块复用的价格输入。"

verification_basis:
  - "检查目标标的可以拉取到 daily 行情。"
  - "检查输出包含撮合所需关键价格字段。"
  - "检查 world service 能读取这些行情。"

test_cases:
  normal:
  - 正常路径：日线行情拉取 的核心场景可以完成。
  exception:
  - 异常路径：日线行情拉取 失败时返回可解释结果。
