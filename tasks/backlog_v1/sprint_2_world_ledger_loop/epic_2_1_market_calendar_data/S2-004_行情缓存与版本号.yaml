task_id: S2-004
task_name: 行情缓存与版本号
sprint: Sprint 2
epic: Epic 2.1 市场日历与行情
story_id: S2-004
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 给 world snapshot 提供缓存和 dataVersion。
business_value: 让 行情缓存与版本号 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- 行情缓存与版本号 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/world/行情缓存与版本号.py'
out_of_scope:
- 不在本 Story 中实现 行情缓存与版本号 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 行情缓存与版本号 之外的后续能力。
- 不做架构级重构。
dependencies:
- S2-003
related_files:
- apps/api/src/modules/world/行情缓存与版本号.py
- apps/api/src/modules/world/service.py
primary_files:
- apps/api/src/modules/world/行情缓存与版本号.py
secondary_files:
- apps/api/src/modules/world/service.py
story_inputs:
  - "S2-003 拉取的日线行情结果。"
  - "world snapshot 对缓存和 dataVersion 的要求。"
  - "同一交易日多次读取行情的复用需求。"

story_process:
  - "为行情结果建立缓存。"
  - "生成可追踪的 dataVersion。"
  - "把缓存和版本号注入 world snapshot。"

story_outputs:
  - "带缓存和 dataVersion 的行情快照。"
  - "可供后续决策和调试复盘使用的版本信息。"

verification_basis:
  - "检查同一批行情可被缓存复用。"
  - "检查 world snapshot 包含 dataVersion。"
  - "检查版本号可用于区分不同数据批次。"

test_cases:
  normal:
  - 正常路径：行情缓存与版本号 的核心场景可以完成。
  exception:
  - 异常路径：行情缓存与版本号 失败时返回可解释结果。
