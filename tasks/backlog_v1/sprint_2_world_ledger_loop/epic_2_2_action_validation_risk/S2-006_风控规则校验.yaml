task_id: S2-006
task_name: 风控规则校验
sprint: Sprint 2
epic: Epic 2.2 动作校验与风控
story_id: S2-006
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 校验 maxPositionPct、maxHoldDays、turnover 等规则。
business_value: 让 风控规则校验 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- 风控规则校验 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/orders/风控规则校验.py'
out_of_scope:
- 不在本 Story 中实现 风控规则校验 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 风控规则校验 之外的后续能力。
- 不做架构级重构。
dependencies:
- S2-005
related_files:
- apps/api/src/modules/orders/风控规则校验.py
- docs/contracts/agent_contract_schema.json
primary_files:
- apps/api/src/modules/orders/风控规则校验.py
secondary_files:
- docs/contracts/agent_contract_schema.json
story_inputs:
  - "已通过基础结构校验的 actions。"
  - "maxPositionPct、maxHoldDays、turnover 等风险规则。"
  - "Agent profile 中的 riskControls 和当前组合状态。"

story_process:
  - "对 actions 执行风险规则校验。"
  - "识别违反仓位、持有期和换手约束的动作。"
  - "把风控判断结果传递给下游执行链路。"

story_outputs:
  - "风控通过的 actions。"
  - "带拒绝原因的风控校验结果。"

verification_basis:
  - "检查违反 riskControls 的动作会被拦截。"
  - "检查通过的动作可以继续进入下一步。"
  - "检查拒绝结果包含可解释原因。"

test_cases:
  normal:
  - 正常路径：风控规则校验 的核心场景可以完成。
  exception:
  - 异常路径：风控规则校验 失败时返回可解释结果。
