task_id: S2-007
task_name: 现金 / 仓位 / lot size 校验
sprint: Sprint 2
epic: Epic 2.2 动作校验与风控
story_id: S2-007
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 校验是否可下单。
business_value: 让 现金 / 仓位 / lot size 校验 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- 现金 / 仓位 / lot size 校验 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/orders/现金_仓位_lot_size_校验.py'
out_of_scope:
- 不在本 Story 中实现 现金 / 仓位 / lot size 校验 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 现金 / 仓位 / lot size 校验 之外的后续能力。
- 不做架构级重构。
dependencies:
- S2-006
related_files:
- apps/api/src/modules/orders/现金_仓位_lot_size_校验.py
- docs/contracts/agent_contract_schema.json
primary_files:
- apps/api/src/modules/orders/现金_仓位_lot_size_校验.py
secondary_files:
- docs/contracts/agent_contract_schema.json
story_inputs:
  - "风控通过后的 actions。"
  - "当前现金、仓位、lot size 和持仓状态。"
  - "下单前必须满足的资金与持仓约束。"

story_process:
  - "校验是否有足够现金、可卖仓位和正确 lot size。"
  - "过滤掉无法真实执行的动作。"
  - "把可执行动作集交给撮合链路。"

story_outputs:
  - "通过现金、仓位、lot size 校验的 actions。"
  - "带失败原因的可执行性校验结果。"

verification_basis:
  - "检查超现金、超仓位或 lot size 不合法的动作会被拦截。"
  - "检查合法动作可继续进入撮合。"
  - "检查失败结果能说明是哪一类约束触发。"

test_cases:
  normal:
  - 正常路径：现金 / 仓位 / lot size 校验 的核心场景可以完成。
  exception:
  - 异常路径：现金 / 仓位 / lot size 校验 失败时返回可解释结果。
