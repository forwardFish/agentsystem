task_id: S2-008
task_name: 收盘价撮合
sprint: Sprint 2
epic: Epic 2.3 撮合与账本
story_id: S2-008
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 实现 MVP 阶段基于 close price 的撮合逻辑。
business_value: 让 收盘价撮合 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 无前置依赖。
acceptance_criteria:
- 收盘价撮合 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/matching/收盘价撮合.py'
out_of_scope:
- 不在本 Story 中实现 收盘价撮合 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 收盘价撮合 之外的后续能力。
- 不做架构级重构。
dependencies:
- 无
related_files:
- apps/api/src/modules/matching/收盘价撮合.py
- apps/api/src/modules/orders/service.py
primary_files:
- apps/api/src/modules/matching/收盘价撮合.py
secondary_files:
- apps/api/src/modules/orders/service.py
story_inputs:
  - "可执行 actions 集合。"
  - "收盘价行情数据。"
  - "MVP 阶段 close price 撮合规则。"

story_process:
  - "按 close price 执行动作撮合。"
  - "为每个可执行动作生成初步撮合结果。"
  - "把撮合结果传递给费用、滑点和结算链路。"

story_outputs:
  - "基于收盘价的撮合结果。"
  - "可继续计算费用和生成 fills 的中间结果。"

verification_basis:
  - "检查动作按收盘价被正确撮合。"
  - "检查无行情或不可执行时不会错误生成撮合。"
  - "检查下游费用与 fill 生成可复用这些结果。"

test_cases:
  normal:
  - 正常路径：收盘价撮合 的核心场景可以完成。
  exception:
  - 异常路径：收盘价撮合 失败时返回可解释结果。
