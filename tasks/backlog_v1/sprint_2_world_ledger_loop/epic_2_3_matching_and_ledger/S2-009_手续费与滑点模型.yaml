task_id: S2-009
task_name: 手续费与滑点模型
sprint: Sprint 2
epic: Epic 2.3 撮合与账本
story_id: S2-009
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 实现 feePct/slipPct。
business_value: 让 手续费与滑点模型 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- 手续费与滑点模型 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/matching/手续费与滑点模型.py'
out_of_scope:
- 不在本 Story 中实现 手续费与滑点模型 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 手续费与滑点模型 之外的后续能力。
- 不做架构级重构。
dependencies:
- S2-008
related_files:
- apps/api/src/modules/matching/手续费与滑点模型.py
- apps/api/src/modules/orders/service.py
primary_files:
- apps/api/src/modules/matching/手续费与滑点模型.py
secondary_files:
- apps/api/src/modules/orders/service.py
story_inputs:
  - "S2-008 的撮合结果。"
  - "feePct、slipPct 等费用与滑点参数。"
  - "结算阶段需要的实际成交成本信息。"

story_process:
  - "根据撮合结果计算手续费和滑点。"
  - "把费用和滑点合并到成交成本中。"
  - "让下游 fill 生成能够基于真实成本结算。"

story_outputs:
  - "带手续费和滑点的撮合结果。"
  - "可供 fill 生成与现金更新使用的成本数据。"

verification_basis:
  - "检查 feePct、slipPct 被正确应用。"
  - "检查成交成本包含费用和滑点。"
  - "检查结算链路可读取这些结果。"

test_cases:
  normal:
  - 正常路径：手续费与滑点模型 的核心场景可以完成。
  exception:
  - 异常路径：手续费与滑点模型 失败时返回可解释结果。
