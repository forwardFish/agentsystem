task_id: S2-011
task_name: portfolio / positions 更新
sprint: Sprint 2
epic: Epic 2.3 撮合与账本
story_id: S2-011
blast_radius: L2
execution_mode: Safe
mode: Safe
goal: 基于 fills 更新持仓与组合。
business_value: 让 portfolio / positions 更新 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- portfolio / positions 更新 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩大到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/matching/portfolio_positions_更新.py'
out_of_scope:
- 不在本 Story 中实现 portfolio / positions 更新 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 portfolio / positions 更新 之外的后续能力。
- 不做架构级重构。
dependencies:
- S2-010
related_files:
- apps/api/src/modules/matching/portfolio_positions_更新.py
- apps/api/src/modules/orders/service.py
primary_files:
- apps/api/src/modules/matching/portfolio_positions_更新.py
secondary_files:
- apps/api/src/modules/orders/service.py
test_cases:
  normal:
  - 正常路径：portfolio / positions 更新 的核心场景可以完成。
  exception:
  - 异常路径：portfolio / positions 更新 失败时返回可解释结果。
