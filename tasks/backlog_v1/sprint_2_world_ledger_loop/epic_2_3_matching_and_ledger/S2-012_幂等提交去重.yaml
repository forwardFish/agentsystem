task_id: S2-012
task_name: 幂等提交去重
sprint: Sprint 2
epic: Epic 2.3 撮合与账本
story_id: S2-012
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 同 idempotency_key 不重复入账。
business_value: 让 幂等提交去重 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- 幂等提交去重 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/matching/幂等提交去重.py'
out_of_scope:
- 不在本 Story 中实现 幂等提交去重 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 幂等提交去重 之外的后续能力。
- 不做架构级重构。
dependencies:
- S2-011
related_files:
- apps/api/src/modules/matching/幂等提交去重.py
- apps/api/src/modules/orders/service.py
primary_files:
- apps/api/src/modules/matching/幂等提交去重.py
secondary_files:
- apps/api/src/modules/orders/service.py
story_inputs:
  - "submit-actions 请求中的 idempotency_key。"
  - "已生成的 fills、订单和结算记录。"
  - "避免重复入账的幂等规则。"

story_process:
  - "识别重复提交的 idempotency_key。"
  - "阻止相同请求重复生成账务结果。"
  - "保留可追踪的去重与复用行为。"

story_outputs:
  - "幂等去重后的提交结果。"
  - "防止重复入账的幂等保护行为。"

verification_basis:
  - "检查相同 idempotency_key 不会重复入账。"
  - "检查重复请求返回稳定结果。"
  - "检查幂等去重行为可被审计。"

test_cases:
  normal:
  - 正常路径：幂等提交去重 的核心场景可以完成。
  exception:
  - 异常路径：幂等提交去重 失败时返回可解释结果。
