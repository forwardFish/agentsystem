task_id: S2-013
task_name: 日终对账校验
sprint: Sprint 2
epic: Epic 2.3 撮合与账本
story_id: S2-013
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 确保 equity = cash + 持仓市值。
business_value: 让 日终对账校验 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- 日终对账校验 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/matching/日终对账校验.py'
out_of_scope:
- 不在本 Story 中实现 日终对账校验 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 日终对账校验 之外的后续能力。
- 不做架构级重构。
dependencies:
- S2-012
related_files:
- apps/api/src/modules/matching/日终对账校验.py
- apps/api/src/modules/orders/service.py
primary_files:
- apps/api/src/modules/matching/日终对账校验.py
secondary_files:
- apps/api/src/modules/orders/service.py
story_inputs:
  - "更新后的现金、持仓和组合数据。"
  - "equity = cash + 持仓市值 的对账规则。"
  - "日终结算后需要输出的对账结果。"

story_process:
  - "根据现金和持仓市值计算 equity。"
  - "校验组合结果是否满足对账公式。"
  - "把对账失败场景显式记录下来。"

story_outputs:
  - "日终对账结果。"
  - "可供 run log 和 daily summary 使用的对账状态。"

verification_basis:
  - "检查 equity 与 cash、持仓市值满足对账关系。"
  - "检查不一致场景会被明确标记。"
  - "检查对账结果可被后续摘要与审计复用。"

test_cases:
  normal:
  - 正常路径：日终对账校验 的核心场景可以完成。
  exception:
  - 异常路径：日终对账校验 失败时返回可解释结果。
