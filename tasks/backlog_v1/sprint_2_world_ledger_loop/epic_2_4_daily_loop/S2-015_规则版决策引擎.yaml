task_id: S2-015
task_name: 规则版决策引擎
sprint: Sprint 2
epic: Epic 2.4 Daily Loop
story_id: S2-015
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: MVP 用 rule-based 产生 actions。
business_value: 让 规则版决策引擎 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- 规则版决策引擎 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/loop/规则版决策引擎.py'
out_of_scope:
- 不在本 Story 中实现 规则版决策引擎 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 规则版决策引擎 之外的后续能力。
- 不做架构级重构。
dependencies:
- S2-014
related_files:
- apps/api/src/modules/loop/规则版决策引擎.py
- apps/api/src/modules/world/service.py
primary_files:
- apps/api/src/modules/loop/规则版决策引擎.py
secondary_files:
- apps/api/src/modules/world/service.py
story_inputs:
  - "S2-014 产出的 context pack。"
  - "MVP 阶段 rule-based 的决策规则。"
  - "submit-actions 需要的动作结构。"

story_process:
  - "基于 context pack 运行规则版决策。"
  - "生成买卖或持有动作。"
  - "保证输出动作满足 submit-actions 契约。"

story_outputs:
  - "rule-based 产生的 actions。"
  - "可进入校验、撮合和结算链路的动作集。"

verification_basis:
  - "检查 context pack 能产生规则动作。"
  - "检查动作结果符合 submit-actions 契约。"
  - "检查后续校验链路可以继续消费这些 actions。"

test_cases:
  normal:
  - 正常路径：规则版决策引擎 的核心场景可以完成。
  exception:
  - 异常路径：规则版决策引擎 失败时返回可解释结果。
