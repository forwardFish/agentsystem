task_id: S2-016
task_name: Daily Loop 编排
sprint: Sprint 2
epic: Epic 2.4 Daily Loop
story_id: S2-016
blast_radius: L2
execution_mode: Safe
mode: Safe
goal: 串起 context -> decide -> risk -> match -> settle。
business_value: 让 Daily Loop 编排 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- Daily Loop 编排 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/loop/daily_loop_编排.py'
out_of_scope:
- 不在本 Story 中实现 Daily Loop 编排 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 Daily Loop 编排 之外的后续能力。
- 不做架构级重构。
dependencies:
- S2-015
related_files:
- apps/api/src/modules/loop/daily_loop_编排.py
- apps/api/src/modules/world/service.py
primary_files:
- apps/api/src/modules/loop/daily_loop_编排.py
secondary_files:
- apps/api/src/modules/world/service.py
story_inputs:
  - "context、actions、风险校验、撮合和结算各阶段能力。"
  - "daily loop 需要串联的主链路节点。"
  - "每日运行应保留的阶段结果和错误边界。"

story_process:
  - "串起 context -> decide -> risk -> match -> settle。"
  - "让各阶段按固定顺序执行并传递结果。"
  - "在运行失败时保留可追踪阶段信息。"

story_outputs:
  - "完整 daily loop 运行编排。"
  - "一条可回放、可审计的每日运行主链路。"

verification_basis:
  - "检查主链路能按顺序执行。"
  - "检查阶段输出会被传给下游。"
  - "检查失败场景能保留明确阶段信息。"

test_cases:
  normal:
  - 正常路径：Daily Loop 编排 的核心场景可以完成。
  exception:
  - 异常路径：Daily Loop 编排 失败时返回可解释结果。
