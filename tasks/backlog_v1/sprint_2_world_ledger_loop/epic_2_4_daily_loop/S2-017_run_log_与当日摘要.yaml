task_id: S2-017
task_name: Run Log 与当日摘要
sprint: Sprint 2
epic: Epic 2.4 Daily Loop
story_id: S2-017
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 生成 daily summary 和审计日志。
business_value: 让 Run Log 与当日摘要 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- Run Log 与当日摘要 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/loop/run_log_与当日摘要.py'
out_of_scope:
- 不在本 Story 中实现 Run Log 与当日摘要 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 Run Log 与当日摘要 之外的后续能力。
- 不做架构级重构。
dependencies:
- S2-016
related_files:
- apps/api/src/modules/loop/run_log_与当日摘要.py
- apps/api/src/modules/world/service.py
primary_files:
- apps/api/src/modules/loop/run_log_与当日摘要.py
secondary_files:
- apps/api/src/modules/world/service.py
test_cases:
  normal:
  - 正常路径：Run Log 与当日摘要 的核心场景可以完成。
  exception:
  - 异常路径：Run Log 与当日摘要 失败时返回可解释结果。
