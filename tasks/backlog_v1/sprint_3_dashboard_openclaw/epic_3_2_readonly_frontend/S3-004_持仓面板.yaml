task_id: S3-004
task_name: 持仓面板
sprint: Sprint 3
epic: Epic 3.2 只读前端
story_id: S3-004
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 展示当前持仓。
business_value: 让 持仓面板 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 无前置依赖。
acceptance_criteria:
- 持仓面板 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩大到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/web/src/app/(dashboard)/agents/[agentId]/page.tsx'
out_of_scope:
- 不在本 Story 中实现 持仓面板 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 持仓面板 之外的后续能力。
- 不做架构级重构。
dependencies:
- 无
related_files:
- apps/web/src/app/(dashboard)/agents/[agentId]/page.tsx
- apps/web/src/features/agent-observation/AgentObservation.tsx
primary_files:
- apps/web/src/app/(dashboard)/agents/[agentId]/page.tsx
secondary_files:
- apps/web/src/features/agent-observation/AgentObservation.tsx
test_cases:
  normal:
  - 正常路径：持仓面板 的核心场景可以完成。
  exception:
  - 异常路径：持仓面板 失败时返回可解释结果。
