task_id: S4-002
task_name: batch agent creation
sprint: Sprint 4
epic: Epic 4.1 种子 Agent 与批量供给
story_id: S4-002
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 支持按模板或 profile 批量创建 Agent。
business_value: 让 batch agent creation 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- batch agent creation 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/agents/bootstrap.py'
out_of_scope:
- 不在本 Story 中实现 batch agent creation 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 batch agent creation 之外的后续能力。
- 不做架构级重构。
dependencies:
- S4-001
related_files:
- apps/api/src/modules/agents/bootstrap.py
- docs/contracts/trading_agent_profile.schema.json
primary_files:
- apps/api/src/modules/agents/bootstrap.py
secondary_files:
- docs/contracts/trading_agent_profile.schema.json
test_cases:
  normal:
  - 正常路径：batch agent creation 的核心场景可以完成。
  exception:
  - 异常路径：batch agent creation 失败时返回可解释结果。
