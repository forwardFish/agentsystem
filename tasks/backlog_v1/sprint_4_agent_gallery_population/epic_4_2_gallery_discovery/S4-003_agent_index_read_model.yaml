task_id: S4-003
task_name: agent index read model
sprint: Sprint 4
epic: Epic 4.2 广场与发现
story_id: S4-003
blast_radius: L1
execution_mode: Fast
mode: Fast
goal: 构建 Agent 列表页所需的索引读模型。
business_value: 让 agent index read model 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 无前置依赖。
acceptance_criteria:
- agent index read model 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/gallery/agent_index_read_model.py'
out_of_scope:
- 不在本 Story 中实现 agent index read model 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 agent index read model 之外的后续能力。
- 不做架构级重构。
dependencies:
- 无
related_files:
- apps/api/src/modules/gallery/agent_index_read_model.py
- apps/web/src/app/gallery/page.tsx
primary_files:
- apps/api/src/modules/gallery/agent_index_read_model.py
secondary_files:
- apps/web/src/app/gallery/page.tsx
test_cases:
  normal:
  - 正常路径：agent index read model 的核心场景可以完成。
  exception:
  - 异常路径：agent index read model 失败时返回可解释结果。
