task_id: S4-007
task_name: leaderboard projection
sprint: Sprint 4
epic: Epic 4.3 比较与榜单
story_id: S4-007
blast_radius: L2
execution_mode: Safe
mode: Safe
goal: 生成收益、回撤、活跃度等榜单投影。
business_value: 让 leaderboard projection 成为可独立验收、可直接进入 Agent 执行闭环的 Story。
entry_criteria:
- 前置依赖已完成并可用。
acceptance_criteria:
- leaderboard projection 的核心输出可以被独立验收。
- 只跨一个业务边界，不扩展到相邻模块。
- 产物能够被后续 Story 直接复用。
- 正常路径和关键失败路径都有可验证结果。
constraints:
- 每个 Story 默认控制在 0.5 天到 1 天完成。
- 只修改当前 Story 相关文件，不跨多个大模块。
- '优先修改这些 primary files: apps/api/src/modules/leaderboard/leaderboard_projection.py'
out_of_scope:
- 不在本 Story 中实现 leaderboard projection 之外的后续能力。
- 不做架构级重构。
not_do:
- 不在本 Story 中实现 leaderboard projection 之外的后续能力。
- 不做架构级重构。
dependencies:
- S4-006
related_files:
- apps/api/src/modules/leaderboard/leaderboard_projection.py
- apps/web/src/app/gallery/compare/page.tsx
primary_files:
- apps/api/src/modules/leaderboard/leaderboard_projection.py
secondary_files:
- apps/web/src/app/gallery/compare/page.tsx
test_cases:
  normal:
  - 正常路径：leaderboard projection 的核心场景可以完成。
  exception:
  - 异常路径：leaderboard projection 失败时返回可解释结果。
